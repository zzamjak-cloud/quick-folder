name: Release Build

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  release:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          # 테스트를 위해 macOS만 빌드
          - platform: macos-latest
            target: aarch64-apple-darwin

    runs-on: ${{ matrix.platform }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'npm'

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri -> target'

      - name: Install frontend dependencies
        run: npm install

      - name: Extract changelog for this version
        id: changelog
        run: |
          VERSION=${{ github.ref_name }}
          VERSION=${VERSION#v}  # v 접두사 제거

          # CHANGELOG.md에서 해당 버전의 내용 추출
          CHANGELOG=$(awk "/## \[$VERSION\]/,/## \[/" CHANGELOG.md | sed '$d' | tail -n +2)

          if [ -z "$CHANGELOG" ]; then
            CHANGELOG="변경사항은 CHANGELOG.md를 참조하세요."
          fi

          # 멀티라인 출력을 위한 처리
          echo "content<<EOF" >> $GITHUB_OUTPUT
          echo "$CHANGELOG" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
        shell: bash

      - name: Build Tauri app with updater
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'QuickFolder v__VERSION__'
          releaseBody: |
            새로운 QuickFolder 버전이 출시되었습니다! 🎉

            ## 변경사항
            ${{ steps.changelog.outputs.content }}

            ## 설치 방법
            - **Windows**: QuickFolder_x.x.x_x64_setup.exe 다운로드 후 실행
            - **macOS**: QuickFolder_x.x.x_aarch64.dmg 다운로드 후 설치

            앱 내에서 자동 업데이트를 확인하거나 위에서 직접 다운로드하세요.
          releaseDraft: false
          prerelease: false
          includeUpdaterJson: true
          args: --target ${{ matrix.target }}

  update-latest-tag:
    needs: release
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Delete old quickfolder-latest release and tag
        run: |
          gh release delete quickfolder-latest --yes || true
          git push origin :refs/tags/quickfolder-latest || true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create quickfolder-latest tag and release
        run: |
          git tag quickfolder-latest
          git push origin quickfolder-latest

          # macOS와 Windows 빌드가 포함된 원본 릴리스에서 asset 복사
          gh release create quickfolder-latest \
            --title "QuickFolder (Rolling Release)" \
            --notes "항상 최신 버전의 QuickFolder를 제공합니다. 자동 업데이트는 이 릴리스를 참조합니다." \
            --latest=false

          # 원본 릴리스에서 파일 다운로드
          gh release download ${{ github.ref_name }} --pattern "*.dmg" --pattern "*.exe"

          # latest.json과 .sig 파일이 있으면 다운로드 (옵션)
          gh release download ${{ github.ref_name }} --pattern "*.sig" || true
          gh release download ${{ github.ref_name }} --pattern "latest.json" || true

          # 다운로드된 파일들을 quickfolder-latest에 업로드
          for file in *.dmg *.exe *.sig latest.json; do
            if [ -f "$file" ]; then
              gh release upload quickfolder-latest "$file" --clobber
            fi
          done
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
